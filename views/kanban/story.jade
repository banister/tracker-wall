li.story.zoomTarget(class= story.type(), data-targetsize="0.45", data-closeclick="true")
  h5.complexity
    = story.story_type == "feature" ? story.estimate : story.story_type.charAt(0).toUpperCase()
  p.description= story.name
  p.user.legend(class= story.personColor())
  p.user(data-owner= story.owned_by_id)= story.personName(memberships)
